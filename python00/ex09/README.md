# ft_package

This is a simple example package, created from scratch, for the Python Piscine of 42.


#### To generate distribution packages :
    python3 -m pip install --upgrade build
    python3 -m build

#### To install the package :
    pip install dist/*.whl
    pip show -v ft_package

#### To uninstall :
    pip install dist/*.whl



You can see my other Python projects on [my GitHub](https://github.com/jchenstud42/13_python_for_data_science). Cheers!
